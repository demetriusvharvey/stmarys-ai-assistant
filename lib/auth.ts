// Auth is handled via lib/session.ts (custom JWT cookie).
// This file is kept as a placeholder to avoid import errors during migration.
// It can be deleted once all references are confirmed removed.
export {};
